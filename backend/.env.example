# Backend Environment Variables

# ===================================
# DATABASE CONFIGURATION
# ===================================
DUCKDB_PATH=/app/data/spark_sword.duckdb
PROGRESS_DB_PATH=/app/data/progress.duckdb

# ===================================
# API CONFIGURATION
# ===================================
API_HOST=0.0.0.0
API_PORT=8000

# ===================================
# CORS CONFIGURATION
# ===================================
# Comma-separated list of allowed origins
# Development: http://localhost:3000
# Production: All PrepRabbit subdomains
ALLOWED_ORIGINS=http://localhost:3000,https://spark.preprabbit.in,https://python.preprabbit.in,https://azure.preprabbit.in,https://databricks.preprabbit.in

# ===================================
# DEBUG MODE
# ===================================
# Set to "true" to enable /docs and /redoc endpoints
# Set to "false" in production for security
DEBUG=true

# ===================================
# OAUTH (Centralized across all subdomains)
# ===================================
# Get credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# JWT Configuration (MUST be same across ALL PrepRabbit backend services)
JWT_SECRET_KEY=your-secret-key-at-least-32-characters-long
SESSION_EXPIRE_MINUTES=10080

# Cookie Configuration (for cross-subdomain authentication)
COOKIE_DOMAIN=.preprabbit.in
COOKIE_SECURE=true
COOKIE_SAMESITE=lax

# Frontend URLs (for OAuth redirects - supports multiple subdomains)
FRONTEND_URL=http://localhost:3000
ALLOWED_REDIRECT_DOMAINS=localhost:3000,spark.preprabbit.in,python.preprabbit.in,azure.preprabbit.in,databricks.preprabbit.in
